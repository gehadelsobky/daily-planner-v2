import { ScoreWeights } from "@/lib/score/types";

export const DEFAULT_WEIGHTS: ScoreWeights = {
  tasks: 30,
  grow: 20,
  habits: 10,
  exercise: 10,
  grateful: 10,
  water: 20
};

export const SYSTEM_DEFAULT_WATER_TARGET = 8;
